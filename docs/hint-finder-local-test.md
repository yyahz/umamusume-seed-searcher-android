# 技能检索本地测试

- 日期：2026-09-13；Android 模拟器，720×1280。
- WebView：91.0.4472.114；早期功能测试使用 0.1.51 本地调试包；发布检查使用 0.1.52 / 53，关闭调试。
- 构建、APK 签名验证、覆盖安装成功；原有 14 项 Node 测试通过。
- 底部五个入口可见；点击技能在当前页面显示本地内容，底部导航保持可见且技能高亮；切换角色再回技能保留条件与页码，无需登录。
- 初始 300 张卡；全神贯注对应 2 张金技能卡、27 张仅下位白技能卡。
- 下位分页可独立切到第二页；切换圆弧艺术家后回第一页。
- 顺时针◎匹配 17 张，切换直线能手匹配 26 张，旧条件不叠加。
- 查看详情可看到超过卡片预览数量的完整技能；系统返回先关闭详情，再切回进入技能页前的标签。
- 早期独立页面原型曾通过 CDP 断网刷新测试；最终内嵌页面的搜索数据随包提供，外部卡面和图标仍需网络。
- 360 CSS 像素宽度无横向溢出；截图核对了状态栏、工具栏、搜索候选和卡片布局。
- 技能页与原页面同属 MainActivity，通过独立资源域的受限 iframe 隔离 DOM 与样式；未新增原生桥接口。

测试截图与详细结果存于忽略提交的 .local/hints-*.png 和 .local/hints-qa.json。未执行真实设备、其他 WebView 版本或 Android 15 的设备测试。

## 触摸与卡片反馈修正

已用 ADB 实际触摸复现候选项失效：pointerdown 后 WebView 仍合成 mousedown，搜索框 blur 会在 click 前隐藏列表。追加 mousedown 默认行为拦截后，实际连续点选全神贯注、直线能手、百万马力均执行搜索并清空输入，结果分别为 2、26、2 张。

结果概览隐藏；百万马力的 7 张下位白技能卡均有十万马力高亮。360 CSS 像素宽下同列技能框测量均为 150 CSS 像素，无横向溢出。BWIKI 的 sj-an 样式没有固定宽度，本地沿用其底色和圆角并统一同列宽度。截图 .local/hints-lower-fixed.png。

## 分组切换与底部分页

新增筛选下方的金技能／下位白技能切换，仅金技能检索时出现。ADB 实际触摸验证切换、下位第二页、切回金技能再回下位仍保留第二页；仅对应组可见，DOM 仅保留两组各自的底部分页。说明缩短为下位名称和不能获取的上位金技能名称。截图 hints-tier-tabs.png、hints-bottom-pager.png。

## v0.1.52 发布检查

- 版本号 0.1.52、versionCode 53；包名、最低 SDK 24、目标 SDK 35 和两项权限保持不变。
- 手工构建默认关闭应用与 WebView 调试；与 v0.1.51 签名一致，APK 签名及 ZIP 对齐校验通过。
- 18 项 Node 测试通过，覆盖原有因子逻辑、金白技能分组、替换检索、圆圈等级归并和分页。
- 最终发布包在开发模拟器覆盖安装成功，启动并进入内嵌技能页，搜索按钮、卡片及底部导航正常显示。
- APK 中技能资源与源码逐文件哈希一致；未打包本机目录、登录数据、私钥或测试截图。提交身份使用仓库已有 GitHub noreply 邮箱。
- downloads 更新包与 Release 附件使用同一文件。未测试其他真实设备或 Android 15。
